name = "Vinay is a good boy"

print(name.replace("boy", "Men"))
print(name) # Strings are immutable which means that you cannot change them by running functions on them
            # That means the original string "name" remains unchanged because strings are immutable
            # But you can change the values of the string without changing their original string... it will make virtual new string without making any change in original string
